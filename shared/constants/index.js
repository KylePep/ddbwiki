import * as data from './data.json'
import * as moveData from './moveData.json'

export const {
  CHARACTER_TYPES,
  ORIGIN_TYPES,
  ARCHETYPE_TYPES,
  FORM_TYPES,
  ITEM_TYPES,
  ENEMY_TYPES
} = data;

export const { MOVE_TYPES } = moveData
